server:
  port: ${PORT:8080}

spring:
  application:
    name: servicio-historial
  datasource:
    url: jdbc:h2:mem:historial_prod_db
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
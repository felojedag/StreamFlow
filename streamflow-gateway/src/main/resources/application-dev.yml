server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: servicio-usuarios
          uri: http://localhost:8081
          predicates:
            - Path=/api/usuarios/**

        - id: servicio-suscripciones
          uri: http://localhost:8082
          predicates:
            - Path=/api/suscripciones/**